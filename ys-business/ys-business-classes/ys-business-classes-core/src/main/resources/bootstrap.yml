server:
  port: 8200
spring:
  application:
    # 微服务名称
    name: ys-classes
  profiles:
    #    active: test
    active: '@runtime-profile@'
  cloud:
    nacos:
      # 配置中心 & 注册中心 nacos地址
      server-addr: '@config-address@'

